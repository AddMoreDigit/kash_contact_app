/**
 * HTTP API for authentication endpoints
 * Creates an API Gateway HTTP API with routes to the auth Lambda function
 */

// For Amplify Gen 2, we define the API as a simple object
// The backend will create the necessary API Gateway resources
export const api = {};
